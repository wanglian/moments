<% if notice %><p style="color: green" class="notice"><%= notice %></p><% end %>

<% content_for :title, "Moments" %>

<div id="notes">
  <% if params[:pinned] %>
    <h2>📌 Pinned Notes</h2>
  <% end %>

  <% @notes.each do |note| %>
    <section>
      <%= render note %>
    </section>
  <% end %>
</div>
